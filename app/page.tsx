import StopLossPositionSizeCalculator from "./StopLossPositionSizeCalculator";

export default function Page() {
  return (
    <div style={{ minHeight: "100vh", background: "black", color: "white" }}>
      <StopLossPositionSizeCalculator />
    </div>
  );
}